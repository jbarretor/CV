export interface Contact {
    id: string
    key: string
    title: string
    fullnamePlaceHolder: string
    emailPlaceHolder: string
    phonePlaceHolder: string
    messagePlaceHolder: string
    submitBtnContact: string
    submitContact: string
}
